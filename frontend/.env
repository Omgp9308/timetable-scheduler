# Environment variables for the React application.
#
# In Create React App, variables must be prefixed with REACT_APP_
# to be accessible in the code via process.env.REACT_APP_VARIABLE_NAME.

# The full base URL for the backend Flask API server.
# This allows you to easily change the API endpoint for different environments
# (e.g., development, production) without changing the source code.
REACT_APP_API_BASE_URL=http://localhost:5001/api
